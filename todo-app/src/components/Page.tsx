import React from "react";
import { Link } from "react-router-dom";

type Props = {
  isActive: boolean;
  to: string;
  children: string;
};

const Page = ({ isActive, to, children }: Props) => {
  return (
    
  );
};

export default Page;
